name: membership_app2
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.7.0
  flutter_riverpod: ^2.6.1

  core:
    path: ../../packages/core

dev_dependencies:
  flutter_test:
    sdk: flutter
  dart_code_linter: ^1.1.3
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.14.1
  import_sorter: ^4.6.0

flutter:
  uses-material-design: true
